このコンテンツは、 [Roy Mendelssohnからのメッセージ ERDDAP ユーザーグループ](https://groups.google.com/g/erddap/c/H-vJoGP42TI) お問い合わせ

ランニング ERDDAP™ クラウドは話題になった。 私はそれに注意する必要があります ERDDAP™ クラウドでは、商用クラウドプロバイダが提供するサーバーではなく、常にクラウド上で実行し、実行するための主要なインピーダンス ERDDAP™ 市販のクラウドプロバイダーでは、S3 ストレージを使用する場合、通常の Linux ブロックへのアクセスを許可しない。 商用クラウドサーバー上で実行するよりも、商用クラウドプロバイダが提供するブロックアクセスオプションを使用するため、基本的には、もちろんコストを除いて、あなた自身の機器上で実行と同じです。

2025年12月1日、私は「rclone and S3」と書いており、これはフォローアップです。 そのメールにGOES17のwathesをマウントし、ファイルをチェックしましたが、私はすべての方法でそれを取らなかった ERDDAP™ 全てがスムーズに動くように見えます。 そして、はい、あなたは家でこれを試すことができますし、あなたは弁護士や医療顧問に相談する必要はありません、それはすべて安全である必要があります。 NCDC OIマウント sst AWS にある avhrr v2.1 で設定 ERDDAP™ 結果を表示します。

- ステップ1: rcloneのエンドポイントを定義する

rclone config は oi を作成します sst s3 \\
プロバイダー AWS \\
地域 私たちイースト-1 \\
所在地_constraint us-east-1 \\
env_auth 偽 \\
匿名 真実


- ステップ2:データセット用のマウントポイントを作成する

sudo mkdir -p /mnt/oiの sst 
sudo chown "$USER:$USER" /mnt/oi sst 

- ステップ3:S3ストレージをマウントしてマウントポイント

許可、許可、許可、許可 (あなたが知っているならば、あなたはボールマーを盗むために謝罪で、) ,

マウントは、ユーザーがどのユーザーがあなたの tomcat を実行するか、通常ユーザーの "tomcat" がデータにアクセスできるように行わなければなりません。 ‘rclone’ は、マウントコマンドを実行し、ユーザーのホームディレクトリに情報を格納したいユーザーの所有者とグループでデータセットをマウントします。 (これは、システムレベルのプロセスとして設定した場合、おそらく軽減されます - 以下を参照してください。) お問い合わせ そのため、マウントコマンドを 'tomcat' として実行できますが、Tomcat に別のユーザとしてマウントコマンドを実行する必要のあるホームディレクトリがない場合。 ヒューズを編集する。 conf ファイル:

1. sudo vi /etc/fuse.conf ディレクティブ

2. 非コメントまたは追加:

user_allow_other ディレクティブ

3. 保存して終了します。


実際のデータは複数のレイヤーの深さで、トップレベルではなくデータレベルにマウントされ、コマンドを tmux ターミナルで実行しているので、コマンドは実行し続けます。

rclone -vv マウント oi sst :noaa-cdr-sea-surface-temp-optimum-interpolation-pds/data/v2.1/avhrr /mnt/oiの sst ツイート
--read-only \\
--allow-other ・・・
--vfs-cache-mode フル \\
--vfs-cache-max-size 1G \\
--vfs-cache-poll-interval 1m \\
--vfs-read-chunk-size 64M \\
--vfs-read-chunk-size-limit 1G \\
--vfs-read-aheadの 256M \\
--バッファサイズ 64M \\
--dir-cache-time 24h \\
--attr-timeout 1s \\
--no-modtime ディレクティブ


- ステップ4:GenerateDatasetsを使う Xml は普通、

使用条件 EDDGrid FromNcFiles はデータ型として、ディレクトリは /mnt/oi です。 sst .... 初期パスはかなり良く、問題なく機能しました。 GenerateDatasets を実行しているときに実行できる xml snippet に 3 つの変更を行いました。 Xml とこれらは:

1. データセット ID を oi に変更 sst ログイン

2. ディレクトリには "" で終わるファイルのミックスが含まれています .nc 「その他」で終わること .nc 「元だけが希望する」 これを行うには、ファイル名のregexを変更します。

 <fileNameRegex> ログイン sst -avhrr-v02r01\\.\\d&#123;8&#125;\\ .nc  </fileNameRegex> 

私はしばしば、私は人生の神秘の1つであるために正規品を見つけると述べています, そして、正規表現を行うためのより良い方法があるかもしれません. しかし、これは働いた

3. ioos_category がセットされていないので、追加しました。

恒久的な制作のために、 xml のスニペット より完全なためにもう少し編集を使用することができます。

- ステップ5: xmlスニペットを追加 datasets.xml フラグを設定

これは、最初のパスにロードするのに時間がかかるので、残りの日に行うために他のものを見つける。

最終結果は:

 https://upwell.pfeg.noaa.gov/erddap/griddap/oisst_rclone.graph
 

痛すぎてしまいませんでした。

結果で再生する場合は、まず、 rclone の設定が最初に推測され、最適化のためにテストする必要があります。 当社グループのジョナサン・シャーマンがこれを見て、IOOS DMAC会議で話しているかもしれません。 また、VM のセットアップをオーケストする方法、GCP の階層的な名前空間を持つ S3 の Bucket を設定するなど、Google Cloud Platform のセットアップに関連する多くのトピックがより速く、もう少し高価です。処理スクリプトを実行すると、GCP で提供されるデータを更新できます。 ERDDAP™ セットアップ方法 このトピックが興味を持っている場合は、話を聞いてください。 ザ・オブ・ザ・ ERDDAP™ 立ち上がって走っているだけ、外から瞬間にアクセスできない NMFS ネットワーク

AWS S3 の Bucket をマウントする AWS VM ではありません。これは、サーバーの 1 つであり、その日のパイプラインは完全に飽和しています。そのため、以前のセットアップが私が行ったものよりも速くなることを期待しています。 (よく私たちのパイプはそれほど大きくはありません - 感謝 NMFS &#33; - しかし、我々は今まで飽和 - データの需要は驚異的です) お問い合わせ

最後に、あなたは不思議かもしれません - 私は自分自身を転がしたいです、私はこれに加えて始めますか? LLMが得意としているのは、よく知られ、よく文書化されている情報であり、私がチェックしたAI (トークンは全部あります&#33;) rclone と AWS と GCP がかなりよく知っているので、ほとんどの設定を行えます。 実際、私はデモに良いデータセットを探していたし、AIは私にいくつかの提案を与え、自分のセットアップのためにいくつかの編集をしましたが、上記のものの大部分を生成しました。

また、Seth は、現在のバージョンの S3 を新規に書いたことを覚えておいてください。 (2.30の) インフォメーション ERDDAP™ - 私は速度を比較していません、そして私はあなたがそれぞれのことをしているかに応じて想像して、その利点を持っています。 既存のものを積み込むため ERDDAP™ rclone を使用して取付けはプロセスを簡単にできます。

ログイン

PS - そして、 rclone は、ベンダーの広範な配列で動作します。これは AWS に制限されず、異なるベンダーに対して「 rclone config 」設定へのいくつかの変更のみが必要です。


システムサービスを作る (ユーザー等に適した変更) : : :
—————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————

[ユニット]
説明=クローンマウント NOAA AWS上のOISST
募集=network-online .tar お問い合わせ
After=networkオンライン .tar お問い合わせ

[サービス]
タイプ=notify
ユーザー=yourUser
グループ:yourGroup

ExecStart=/usr/bin/rclone マウント oi sst :noaa-cdr-sea-surface-temp-optimum-interpolation-pds/data/v2.1/avhrr /mnt/oiの sst ツイート
--read-only \\
--allow-other ・・・
--dir-perms 0755 \\
--file-perms 0644 \\
--vfs-cache-mode フル \\
--vfs-cache-max-size 1G \\
--vfs-cache-poll-interval 1m \\
--vfs-read-chunk-size 64M \\
--vfs-read-chunk-size-limit 1G \\
--vfs-read-aheadの 256M \\
--バッファサイズ 64M \\
--dir-cache-time 24h \\
--attr-timeout 1s \\
--no-modtime ディレクティブ

ExecStop=/bin/fusermount ディレクティブ -uz /mnt /oi sst 
リスタート=オンファイヤー
リスタートSec=10

[インストール]
WantedBy=マルチユーザ .tar お問い合わせ
